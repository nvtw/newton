Add opt-in speculative rigid contacts through the `CollisionPipeline(max_speculative_extension=...)` constructor argument and per-call `CollisionPipeline.collide(..., dt=...)` horizon overrides, with continuous swept-AABB filtering and predictive contact reduction, and reject unsupported hydroelastic combinations.
