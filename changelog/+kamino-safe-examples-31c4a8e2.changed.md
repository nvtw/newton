Improve Kamino DVI robustness and performance, including contact allocation, warm-starting, factorization, and articulated sparse CUDA solves, and enable it in the selection cartpole example. The Schur-complement contact path is available through `use_schur_complement=True`; the alternating path remains the default. Review custom tuning because the default for `omega` is now `1.2`, and contact warm-starting now includes a net-force fallback.
