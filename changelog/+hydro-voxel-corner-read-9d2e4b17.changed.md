Speed up hydroelastic marching-cubes contact generation by reading all eight corner SDF values of a voxel with a single subgrid lookup instead of eight independent texel lookups. Contacts are unchanged for voxels inside the SDF narrow band; corners on a face shared with a coarse-only neighbor block now use the block's own fine border sample, which is the more accurate value. No migration is needed.
