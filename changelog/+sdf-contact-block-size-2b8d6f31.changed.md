Speed up mesh-mesh SDF contact generation by running the edge kernels with 128-thread blocks (four resident blocks per SM) and four launch waves of edge chunks instead of 256-thread blocks with two waves. The nut/bolt SDF scene collides 5-14% faster across 20 to 2000 worlds with identical contacts; no migration is needed.
