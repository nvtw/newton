Accelerate `ModelBuilder.replicate()` and `ModelBuilder.finalize()` for many-world scenes by holding replicated builder fields as NumPy arrays and materializing them as lists only when they are read. Replication, finalization, and on-demand list materialization also pause Python's cyclic garbage collector for their duration, restoring its previous state before returning or propagating an exception. Public APIs are unchanged and no migration is required.
