# Connected normal block: frozen diagnostic, stale input

The saved mode-continuation245_1 snapshot predates current source validation. Its unequal endpoint world witnesses generate a real angular impulse defect. Results below must not be attributed to the current solver. Parent queued a current-source245_1 capture after GPU regression53942 releases.

The existing reference_high_mass_connected_normals.py already alternates exact connected normals with metric tangent updates. Its biased reference uses radius mu*lambda_n, while production excludes Baumgarte load with clamp(lambda_n + mass_coeff*eff_n*bias,0,lambda_n). That earlier biased experiment is not native-law equivalence.

prototype_connected_normal_sweeps.py evaluates saved unbiased relaxation245_1: zero regularization and PD required; speculative rows remain fixed; friction comes from headers; each selected row has exactly one owner. Both variants execute eight sweeps, with identical ordered metric tangent updates. Connected normals suppress scalar normal updates. This is not a live8x8 trajectory benchmark.

Eight-sweep normal residual: scalar .0467679 versus connected .0394147 m/s. Tangent fixed-point residual: .0439566 versus .0110747 m/s. Energy changes: -.0417292 versus -.1959333 J. Work mismatch below2.5e-16 J; linear momentum plus static reaction below2.4e-15 Ns.

Angular momentum fails a strict common-point gate: scalar8.72e-9 versus connected6.68e-7 Nms maximum. The defect equals the torque of stored unequal world witnesses within3.4e-15 Nms. Larger impulses amplify old geometry error. Do not recenter witnesses silently or claim conservation success.

Artifacts /tmp/high_mass_normal_sweeps_native_relax.json and .npz. Input arrays unchanged; no canonical edits or GPU kernels. No live RMS or performance claim. The CPU oracle enumerates256 masks for8 contacts per sweep: same iteration count does not mean same arithmetic budget.

Before live injection: refresh current snapshot and check common points; preserve actual biased coefficients and bias-adjusted friction load; replace owned normal updates rather than double-solving; preserve speculative/PD behavior or explicitly reject unsupported modes. Maintain8x8 temporal/iteration settings.

## Current-source refresh

Current capture at245_1 uses actual solve buffers (8 points, unsplit/unpacked); max common-point mismatch57.085nm. /tmp/high_mass_current_245_1_capture.json records source hashes. Eight frozen sweeps scalar versus connected: normal .128321 versus .117616m/s; tangent .041169 versus .023976m/s. Work balance below2.3e-15J, linear reaction balance below3e-15Ns. Angular defect1.13e-8 versus3.35e-7Nms is completely explained by stored FP32 lever mismatch within5.4e-15Nms. No witness recentering or tolerance relaxation.

Normal matrix rank6/8, condition6.64e16; maximum impulse3.74 versus12.87Ns. Top Z speed improves -.036923 to-.0000802m/s, but dominant lateral Y speed remains .21056 versus .21114m/s. Earlier message incorrectly called the Y component vertical; this document uses the benchmark's Z-up convention. Thus normal coupling addresses vertical closure but not dominant lateral slip. No live RMS claim.
